spring:
  datasource:
    url: jdbc:h2:mem:agents;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password: ''
  flyway:
    locations: classpath:db/migration,classpath:db/local
agent:
  provider: STUB
